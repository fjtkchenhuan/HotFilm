<?xml version="1.0" encoding="utf-8"?>
<manifest package="app.com.example.android.hotfilm"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.SettingActivity" />
        <activity
            android:name=".activity.FilmDetailActivity"
            android:label="@string/film_detail"
            android:theme="@style/MyAppTheme" />
        <activity
            android:name=".activity.CollectActivity"
            android:theme="@style/dialog" />
        <activity
            android:name=".activity.FilmLovedActivity"
            android:label="@string/action_kind">

        </activity>
    </application>
</manifest>